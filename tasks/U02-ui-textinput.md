# U02 — TextInput / FormField

- **Wave:** 2c · **Module:** ui · **Lang:** TS/React · **Depends on:** F07

## Owns
- `frontend/src/components/TextInput.tsx`

## Steps
1. Labeled input: `label`, `error`, `hint`, `required`; label↔input id; `aria-invalid`/`aria-describedby`.

## Acceptance
- [ ] Error state + message; accessible label association.
